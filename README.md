# Pancham-hotel-app

Branching Strategy

PR workflow

Conflict resolution

Release management

“Implemented a Git-based version control workflow using feature, develop, release, and hotfix branches. Enforced pull requests with code reviews, resolved merge conflicts, and managed production-ready releases with tagging to ensure stable deployments.”
